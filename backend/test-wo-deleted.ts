import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const wos = await prisma.workOrder.findMany(); console.log(Total WOs: ); const deletedWos = wos.filter(w => w.deletedAt !== null); console.log(Deleted WOs: ); if (deletedWos.length > 0) { await prisma.workOrder.updateMany({ data: { deletedAt: null } }); console.log('Restored work orders!'); } } main().finally(() => prisma.$disconnect());
