import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const wos = await prisma.workOrder.findMany({ include: { organization: true } }); console.log(Found  work orders); if (wos.length > 0) { console.log('Sample WO orgId:', wos[0].organizationId, 'deletedAt:', wos[0].deletedAt, 'Org name:', wos[0].organization?.name); } } main().finally(() => prisma.$disconnect());
