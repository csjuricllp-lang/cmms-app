import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const workspaceOrg = await prisma.organization.findFirst({ where: { name: 'Workspace Inc.' } }); if (!workspaceOrg) { console.log('Not found'); return; } await prisma.userOrganization.updateMany({ data: { organizationId: workspaceOrg.id } }); console.log('Updated to Workspace Inc.'); } main().finally(() => prisma.$disconnect());
