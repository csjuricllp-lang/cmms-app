import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const models = ['user', 'asset', 'location', 'workOrder', 'vendor', 'customer', 'part', 'purchaseOrder', 'checklist', 'pMSchedule', 'maintenanceRequest', 'team', 'organization']; for (const m of models) { try { await (prisma as any)[m].updateMany({ data: { deletedAt: null } }); console.log('Restored', m); } catch (e) { console.log('Skipping', m); } } } main().finally(() => prisma.$disconnect());
