import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function main() { const role = await prisma.role.findFirst({ where: { name: 'Administrator' } }); if (!role) { console.log('No Admin role found'); return; } await prisma.userOrganization.updateMany({ data: { roleId: role.id } }); console.log('Updated user orgs with Admin role'); } main().finally(() => prisma.$disconnect());
